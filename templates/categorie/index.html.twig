{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie in categories %}
            <tr>
                <td>{{ categorie.id }}</td>
                <td>{{ categorie.titre }}</td>
                <td>
                    <a href="{{ path('app_categorie_show', {'id': categorie.id}) }}"><button type="button" class="btn btn-primary">Afficher</button></a>
                    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}"><button type="button" class="btn btn-info">Editer</button></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_new') }}"><button type="button" class="btn btn-primary">Créer</button></a>
{% endblock %}
